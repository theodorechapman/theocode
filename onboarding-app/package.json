{
  "name": "theocode",
  "version": "0.1.0",
  "private": true,
  "description": "theocode setup: connect the grok CLI, Supabase MCP, and Vercel MCP via OAuth",
  "main": "dist/main.js",
  "scripts": {
    "build:main": "bun build src/main.ts src/preload.ts --outdir dist --target node --format cjs --external electron",
    "build:renderer": "bun build src/renderer/renderer.ts --outdir dist/renderer --target browser && cp src/renderer/index.html src/renderer/app.css dist/renderer/ && cp -R src/renderer/assets dist/renderer/",
    "build": "bun run build:main && bun run build:renderer",
    "start": "bun run build && electron .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^33.0.0",
    "typescript": "^5.6.0"
  }
}